<form class="blogger" action="{{ path('BlogBundle_comment_create', {'post_id': comment.post.id}) }}" method="post" {{ form_enctype(form) }}>

	{{ form_rest(form) }}

	<input type="submit" value="Отправить">
</form>